# Entorno
NODE_ENV=development
PORT=3001

# CORS: ajustar al dominio del Frontend en prod
CORS_ORIGIN=http://localhost:5173

# Base de datos MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=changeme
DB_NAME=agromanager
DB_POOL_LIMIT=10
